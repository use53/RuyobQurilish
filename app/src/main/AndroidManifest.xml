<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.ruyobqurilish.myruyobqurilish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ruyob"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRuyobQurilish">
        <activity android:name=".registratsiya.RegistratsiyaActivity"
            android:theme="@style/Theme.MyRuyobQurilish.NoActionBar"
            />

        <activity
            android:name=".ui.HomeActivity"
            android:theme="@style/Theme.MyRuyobQurilish.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.MyRuyobQurilish.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>